package redis.redis命令;

public class list {
    /*
        List类型
    Redis中的List类型与Java中的LinkedList类似，可以看做是- -个双向链表结构。既可以支持正向检索和也可以支持反向
    检索。
    特征也与LinkedList类似: .
    ●有序
    ●元素可以重复
    ●插入和删除快
    ●查询速度一般


    List的常见命令有:
    ●LPUSH key element... :向列表左侧插入-一个或多个元素
    ●LPOP key:移除并返回列表左侧的第-个元素，没有则返回nil
    ●RPUSH key element... :向列表右侧插入-一个或多个元素
    ●RPOP key:移除并返回列表右侧的第- -个元素
    ●LRANGE key star end:返回一段角标范围内的所有元素
    ●BLPOP和BRPOP: 与LPOP和RPOP类似，只不过在没有元素时等待指定时间，而不是直接返回nil

     */
}
