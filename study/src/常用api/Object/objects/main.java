package 常用api.Object.objects;

public class main {
    //Objects 是一个工具类
    // public static boolean equals(Object a,Object b)  先做非空判断,比较两个对象
    //细节:
    //1.方法的底层会判断s1是否为nu1l，如果为nu1l，直接返回false
    //2.如果s1不为null，那么就利用s1再次调用equals方法
    //3.此时s1是Student类型，所以最终还是会调用Student中的equals方法。
    // 如果没有重写，比较地址值，如果重写了，就比较属性值。

    // public static boolean isNull(Object obj)         判断对象是否为null,为null返回true,反之
    // public static boolean nonNull(Object a,Object b) 判断对象是否为null,跟isNull 结果相反






}
